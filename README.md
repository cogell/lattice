# Lattice

A flexible, schema-free graph builder where users define their own node and edge types with custom fields, then interact with their data through editable table views, an interactive graph visualization, a REST API, and a CLI — see the full [PRD](./PRD.md) for details.
